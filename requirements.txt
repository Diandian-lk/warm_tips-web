# 核心依赖（必选）
streamlit==1.38.0  # 适配Python 3.13的稳定版本
numpy==1.26.4      # 与contourpy兼容的版本
pandas==2.2.3      # 常用数据处理包（若不需要可删除）
matplotlib==3.8.4  # 可视化包（若不需要可删除）

# 可选依赖（根据应用功能增减）
# 如需其他包，仅添加实际用到的（如 requests、scikit-learn 等）
